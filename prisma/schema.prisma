generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Attraction {
  id         Int     @id @default(autoincrement())
  name       String
  detail     String
  coverImage String
  latitude   Float
  longitude  Float
}
